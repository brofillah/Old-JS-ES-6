// OLD JAVASCRIPT

var user = {
    name: "Farhan",
    age: 25
}

myName = user.name
console.log(myName);

// ES6

const list = {
    name: 'Shopping List',
    items: ['Milk', 'Cow']
}

const {name, items} = list

console.log(name, items);
